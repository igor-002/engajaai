# Product detail — page overrides

Inherits MASTER. Overrides:

- Two-column on `md+`: image gallery left (sticky), info right
- Mobile: stacked, gallery first, info second, description third
- Trust badges row below CTA: "Entrega imediata", "Pagamento seguro", "Suporte 24/7" (3 cards, icons via lucide)
- Description: prose styles (`prose prose-invert max-w-none`) for richtext
- Buy + Add-to-cart buttons side-by-side on `md+`, stacked on mobile (Buy first/primary)
